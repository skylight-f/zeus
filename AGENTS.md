# Zeus 项目代理约束

## 个人偏好

- 日常开发改动与界面调整统一使用 `pnpm dev` 的源码开发模式进行真实运行验证，不默认启动或验收正式 `Zeus.app`。
- 只有用户明确要求打包应用、发布链路或安装包验收时，才使用对应的 `Zeus.app` / `Zeus Test.app` 产物。

## 开发与验证

- 不采用 TDD 或 RED-GREEN-REFACTOR，不把先写失败测试作为实现前置条件。
- 不创建、维护或恢复 Vitest、单元/组件/DOM/CSS 契约测试及 `test/`、`*.test.*` 体系；不默认新增测试文件、脚本、依赖或将其加入 CI、发布门禁。历史测试记录不是现行规则。
- 日常按改动范围执行 `pnpm lint`、`pnpm typecheck`、`pnpm build`；本地与 CI 的完整检查入口为 `pnpm verify:publish`，既有专项探针按需运行。
- 行为变化需要必要的真实运行检查；静态检查、构建和打包成功不能替代运行验收。

## 开发与安装

- 日常真实运行验收使用 `pnpm dev`，直接验证当前工作树源码，并使用独立的 development 数据和凭据。
- `pnpm package:mac` 默认生成测试应用，不生成 DMG；仅在需要验证打包产物时使用 `Zeus Test.app`，bundle ID 为 `dev.hypha.zeus.test`，并使用独立用户数据目录。
- 只有用户明确要求正式 DMG 或正式发布时，才执行 `pnpm package:mac:release` 或正式发布流程生成 DMG；不得因日常开发、构建或验收请求自动生成 DMG。
- 生产身份 `Zeus.app` 只由显式发布链路生成，发布候选只作结构、签名、DMG 和清单校验，不作为真实界面验收包启动。
- 不因修改代码自动覆盖 `/Applications` 中已安装的应用；不自动迁移、覆盖或删除正式用户数据。打包前确认目标产物未运行。
- 静态、构建、打包和真实运行结果分别记录，不把产物校验当成运行验收。
- 验收应用只能使用当前任务正常构建的完整测试包；禁止复制、改名或重签系统命令来拼装可启动的应用样本，禁止通过修改包元信息伪造版本。
- 真实启动验收使用 `scripts/verify-packaged-app-health.mjs` 的 `--runtime-root` 和 `--runtime-pid` 检查本任务进程、真实宿主及推进的心跳；模拟响应或磁盘文件替换不能证明启动成功。发现崩溃即记录失败并停止重复验证。

## 精简治理

- 以源码、配置和实际运行证据为准；不恢复与源码重复的设计书、章节验收矩阵或文件行数门禁。
- 任务文档保留在本地 `docs/`，由 `.gitignore` 排除，不加入提交；已有本地记录应继续使用，不为退出版本管理而删除本地内容。
- README 只维护使用和开发入口；发布正文放在 `releases/`。仅在公共行为、操作入口或重要约束变化时更新对应说明。
- 文档、任务记录、设计说明和代码注释使用简体中文。
- 未经用户明确要求，不执行 git commit、push、merge、revert 等修改历史或远端动作。
