# Zeus 领域语言

Zeus 是本地优先的智能体工作台。本词汇表固定跨任务、会话、运行时和知识管理功能共同使用的领域语言，避免把产品实体、Provider 原生实体和界面投影混为一谈。

## 会话与执行

**产品会话**：
用户看到并长期持有的对话实体，拥有稳定身份、标题、项目或任务归属。
_避免：Thread、Session、Provider 会话_

**运行分段**：
产品会话中一段由同一运行时原生连续执行的区间；切换 Provider 或无法原生续接时会产生新分段。
_避免：新会话、子会话_

**原生会话**：
由 Codex、Pi 等 Provider 拥有并按其恢复协议管理的 thread 或 session。
_避免：产品会话、Zeus 会话_

**提交**：
一次不可变的用户执行意图；编辑、改路由、重试或替换会创建有关联的新提交。
_避免：消息、草稿、轮次_

**命令信封**：
跨入口表达一次副作用意图的版本化身份，明确主体、范围、预期版本和幂等身份。
_避免：请求正文、HTTP 请求、工具参数_

**命令接纳**：
本机执行核心已持久接受命令身份与意图的事实；不表示 Provider 已接纳或副作用已完成。
_避免：HTTP 成功、Provider 接纳、执行完成_

**轮次**：
运行时正式接纳一个提交后，从该输入到本轮终态的执行单位。
_避免：提交、模型请求_

**模型请求**：
轮次内一次真实推理调用，包括工具续跑、重试或上下文整理产生的调用。
_避免：轮次、回复_

**模型历史**：
已被运行时接纳，并允许交给后续模型的有序事实集合。
_避免：可见时间线、完整会话记录_

**便携上下文**：
从确认模型历史的某个水位编译，用于新运行分段继续工作的确定性输入。
_避免：完整历史、长期记忆_

**处理过程项**：
轮次中可供用户理解执行进展的思考摘要、工具、命令、重试、整理或等待记录。
_避免：模型历史、原始 Provider 事件_

## 服务档位

**项目模型速度偏好**：
用户在一个项目内对一个精确模型身份显式选择的 Standard 或 Fast；模型身份由模型来源 ID 与模型 ID 共同组成。偏好保存在项目配置中并供任务推送、项目会话、冲突处理、代码审查和既有会话共同读取，没有记录时按 Standard 运行但不自动补写记录。
_避免：会话类型速度偏好、界面本地存储、同名模型共用、默认值自动记忆_

**请求档位**：
一条会话提交在发送时记录的用户意图；Standard 记录为 `null`，Fast 只记录为精确的 `priority`。它不表示 Zeus 已经向 Provider 下发成功，也不表示 Provider 实际采用。
_避免：推理强度、模型切换、下发档位、Provider 实际档位_

**下发档位**：
Zeus 在调度上下文中经过当前模型能力复验后，实际传给 `thread/start` 与 `turn/start` 的 `serviceTier`；只允许 `null` 或精确的 `priority`。恢复已有线程时先恢复 Provider 原有事实，下一轮才按当前项目模型速度偏好重新计算下发档位。
_避免：请求档位、Provider 实际档位、恢复时覆盖 Provider 事实、Fast 别名_

**Provider 实际档位**：
Codex app-server 在线程设置通知、线程响应或用量事件中报告的真实服务档位；它决定运行详情、用量归档和计费展示，可以与请求档位不同。
_避免：下一轮选择、请求档位、Zeus 推断值_

**服务档位降级**：
用户请求 Fast，但模型目录未声明精确 `priority`、app-server 明确拒绝该档位，或 Provider 实际采用 Standard 时发生的继续执行行为；Zeus 不改变模型和推理强度，不改写项目 Fast 偏好，并在触发消息后留下与提交绑定、可恢复且不冒充智能体回答的系统提示。
_避免：静默回退、失败会话、降低推理强度、切换模型、重复发送用户消息_

## 事实与派生数据

**Provider 运行事实**：
Provider 对原生会话、模型上下文、原始工具事件和恢复身份拥有的权威事实。
_避免：Zeus 业务事实、界面缓存_

**Zeus 业务事实**：
Zeus 对项目、任务、产品会话、提交队列、审批、授权、变更和未读状态拥有的权威事实。
_避免：Provider 运行事实、投影_

**投影**：
为查询或界面展示从权威事实派生的数据，可重建且不得反向覆盖权威事实。
_避免：事实源、原始历史_

**资产**：
由稳定引用定位的完整工具结果、命令输出、附件、差异或其他大型内容。
_避免：内联投影、缓存_

## 知识与记忆

**长期记忆**：
跨任务仍稳定有效的用户偏好、安全边界和工作流规则。
_避免：任务记录、完整会话、运行证据_

**任务文档**：
项目仓库 `/docs` 中记录任务事实、设计决策、阶段结果和验收边界的文档。
_避免：长期记忆、原生会话_

**待核对记忆**：
到达 `review_after` 或来源已无法确认，因而保留审计记录但不得作为当前事实注入的长期记忆。
_避免：自动删除、低置信度猜测、已确认偏好_

**上下文编译**：
按当前项目、任务、操作风险、Provider 能力和分类预算，从有来源版本的候选中确定性选择模型输入。
_避免：拼接全部资料、完整会话回放、系统提示词覆盖_

**运行证据**：
用于追溯具体执行过程的 rollout、日志、工具输出和原生会话片段，默认按需读取。
_避免：长期记忆、默认上下文_

**冷证据索引**：
只保存运行证据来源身份、摘要、状态和精确位置的可重建索引；不复制原始正文。
_避免：第三份完整会话、全文缓存、默认上下文_

## 运行与隔离

**电脑控制归属**：
某个轮次对已观察目标应用的控制关系；不同轮次可以分别控制不同应用，用户接管、停止和画面预览均属于对应轮次。
_避免：整台桌面归某个会话独占、所有会话共用控制画面_

**运行 Profile**：
数据根和运行宿主所属的 distribution 边界，只取 `production`、`test` 或 `development`；它与 bundle ID、Keychain 命名空间和宿主协议身份一起判定能否读写或附着。
_避免：仅依赖目录名、环境变量、进程名或开发/正式猜测_

**数据根身份**：
一个数据根首次受控认领后持久保有的唯一身份，同时绑定规范路径、运行 Profile、bundle 身份语义、结构代次和 Keychain service 身份。production/Test 的 bundle 值是打包产物应核验的 code-sign bundle ID；development 通常运行在共享 Electron.app，因此只使用明示的 development distribution label，不声称它是 Zeus 签名 bundle ID。
_避免：数据目录、Electron userData 路径、可抵抗同 UID 篡改的密码学身份_

**离线数据根认领**：
所有 Zeus/Provider/Execution Host 均退出后，把既有非空、无 marker 的自定义 production/Test 根显式绑定为数据根身份的两阶段维护操作。操作者必须提供 canonical root、Profile、固定 distribution label，并用当前 inventory 计划令牌二次确认；该操作只发布 marker，不迁移、清理或修复业务内容。
_避免：在线补标、按目录内容自动猜 profile、默认读取正式根_
